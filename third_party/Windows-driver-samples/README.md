# Windows-driver-samples (Microsoft)

Upstream: https://github.com/microsoft/Windows-driver-samples

Portions of `driver/src/Driver.cpp`, `driver/src/Driver.h` and
`driver/nyanvdd.inf` are derived from this project's
`video/IndirectDisplay` (IddSampleDriver) sample.

`LICENSE` in this directory is a verbatim copy of the upstream project's
license (Microsoft Public License, MS-PL), kept here so it travels with any
source distribution of this repository.
